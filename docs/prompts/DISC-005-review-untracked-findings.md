# DISC-005 — Review untracked findings

Repository: `ivanjovicic/AgentsWatch`

Review the last five relevant run evidence files and identify useful follow-up items that have no discovery record.

For each item, reuse an existing record when the root issue matches. Otherwise create a record, assign one owner, select a truthful queue state, and add a focused prompt candidate when further work is justified.

Return the reviewed runs, records created or updated, duplicates linked, prompt candidates, and remaining uncertainty. Validate links and run `git diff --check` for changed markdown files.

This is a documentation and routing review only.
